// sections
import ProviderListView from 'src/sections/provider/provider-list-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Provider List',
};

export default function ProviderListPage() {
  return <ProviderListView />;
}
